import {afterEach,describe,expect,it,vi} from "vitest";
vi.mock("server-only",()=>({}));
const original={...process.env};afterEach(()=>{process.env={...original}});
describe("integration health",()=>{it("reports safe server-side statuses without values",async()=>{const {getIntegrationHealth}=await import("./integrationHealth.server");process.env.NEXT_PUBLIC_SUPABASE_URL="https://secret-project.supabase.co";process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY="secret-key";process.env.OPENAI_API_KEY="sk-secret";const serialized=JSON.stringify(getIntegrationHealth());expect(serialized).toContain("Configured");expect(serialized).not.toContain("secret-project");expect(serialized).not.toContain("secret-key");expect(serialized).not.toContain("sk-secret");expect(serialized).toContain("not live-verified")});it("reports missing configuration by safe variable name",async()=>{const {getIntegrationHealth}=await import("./integrationHealth.server");delete process.env.SUNDIAL_ADMIN_URL;expect(getIntegrationHealth().find(item=>item.name==="Sundial Admin URL")).toMatchObject({status:"Missing",variables:["SUNDIAL_ADMIN_URL"]})})});
